import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const _geist = Geist({ subsets: ["latin"] });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: 'Basic Dev\'s | Roblox Game Development Studio',
  description: 'Basic Dev\'s is a Roblox game development studio focused on creating innovative gaming experiences. We invest in talented developers and build exciting games.',
  generator: 'v0.app',
  icons: {
    icon: '/images/logo-transparent.png',
    apple: '/images/logo-transparent.png',
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="bg-background">
      <body className="font-sans antialiased">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
